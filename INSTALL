Installation Notes for `urlview'
===============================

To configure:

	aclocle
	automake -a
	./configure

To use the S-LANG library:

	./configure --with-slang

Build:

	make

Install:

	make install

